<%- include('partials/head.ejs') %>
<%- include('partials/navbar.ejs') %>

<% if (isLoggedIn === false) { %>
  <meta http-equiv="refresh" content="0; URL=/users/login"/>
<% } %>

  <main class="container">
    
    <h4>Añadir nueva imagen:</h4>
    <h6>Pega la url aquí:</h6>
    <form action="/imagenes" method="POST">
        <input type="text" name="url">
        <a class="waves-effect waves-light btn green"><i class="material-icons left">add</i><input type="submit" value="">Añadir</a>
    </form>

    <h1>Imagenes</h1>
        <% for (let imagen of lista_imagenes) { %>
            <p>
                <img width="600px"; src=<%= imagen %>>  
            </p> 
        <% } %>
    </h1>
    
    <a href="/" class="waves-effect waves-light btn green"><i class="material-icons left">arrow_back</i>Volver</a>

    <p></p>
    
  </main>

<%- include('partials/footer.ejs') %>